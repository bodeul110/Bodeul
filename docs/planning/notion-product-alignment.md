# Notion 제품 기준 정합성

기준일: 2026-08-30

## 확인 범위

Notion의 MVP 통합본 v1.1, 화면별 상세 명세 v2.2, 동행 채팅 명세 v1.0, 출시 전 법률 체크리스트 v1.0, `개발 정책 관련 내용`, 보유기간 및 파기정책 v1.0을 현재 저장소 구현과 대조했다. 2026-08-25에는 기획 답변과 2026-08-11 법률 검토 의견서를 다시 확인했다. 비공개 URL, 페이지 ID, 계정 정보, 개인정보와 법률 문서 원본은 이 공개 저장소에 기록하지 않는다.

## 현재 제품 흐름

1. 첫 화면과 권한 안내를 거쳐 환자·보호자 또는 매니저 역할을 선택한다.
2. 매니저는 자격 서류 제출과 승인 단계를 거친다.
3. 환자·보호자는 병원, 일정, 건강·이동 정보를 입력해 동행을 요청한다.
4. 관리자가 승인된 매니저 한 명을 배정한다. 승인 매니저의 공개 일감 목록과 선착순 수락은 MVP에서 제외한다.
5. 매칭 이후 매니저는 동행 가이드를 진행하고 환자·보호자는 진행 상태를 확인한다.
6. 환자·배정 매니저와 `CHAT` 정보공유 동의가 유효한 보호자는 직접 채팅하고 주요 상태 알림을 받는다. 시스템 이벤트 중심 동행방은 과거 제안으로 분류한다.
7. 서비스 종료 후 최종 리포트와 후기·후속 처리를 제공한다.

Figma에는 동행 가이드 1~13 화면이 있으나, 현재 서버 계약은 병원 가이드 데이터의 단계 수를 사용한다. `13단계 고정`은 아직 기술 계약이 아니다.

## 현재 구현과의 정합성

| 항목 | Notion 제품 의도 | 현재 저장소 기준 | 판단 |
| --- | --- | --- | --- |
| 예약·매칭 | 관리자 배정 | PostgreSQL 배정 함수는 관리자 runtime만 실행하며 매니저 self-accept API는 없음 | 관리자 배정으로 통일, 선착순 수락 폐기 |
| 동행 가이드 | 단계별 기록과 보호자 진행 확인 | DB 가이드 단계, 세션 상태와 리포트를 Core API로 관리 | 방향 일치, 단계 명칭 정규화 필요 |
| 동행 채팅 | 환자·배정 매니저·`CHAT` 동의 보호자 직접 채팅 | 직접 채팅, 읽음, FCM, Realtime 구현 | `CARE_ENDED`부터 신규 입력과 매니저 조회 차단, 환자·동의 보호자만 기존 기록 조회 |
| 채팅 첨부 | JPEG·PNG·PDF 최대 3개·각 10 MiB | Core API가 MIME·magic byte·용량·경로를 검증 | 동행 종료·취소 후 30일 보관, `CARE_ENDED` 신규 첨부 차단 보완 |
| 위치 공유 | MVP production GPS와 상봉 1회 좌표 미수집 | 앱·서버에 1회·연속 공유 경로가 있고 production 강제 OFF는 없음 | UI 숨김·서버 `off` 강제 전 운영 기능으로 표시 금지 |
| 환자·보호자 연결 | 구조 미정 | 이메일·전화번호와 예약 참여 관계로 연결 | 기술 연결은 구현, 정보공유 동의는 미정 |
| 취소 | 정책 미정 | `REQUESTED`, `MATCHED` 상태 취소 계약 존재 | 상태 전이는 구현, 환불·노쇼 정책은 미정 |
| 보관 기간 | 위치 미수집, 채팅 180일, 채팅 첨부 30일, 자격 증빙 원본 30일 | 같은 파기 계약과 dry-run/apply 경로 구현 | 정책값 통일, production apply만 출시 게이트 대기 |
| AI 음성·STT | 문서마다 UI 전용과 실제 처리로 충돌 | 실제 음성·STT·AI 요약 미구현 | 후속 범위 |
| OCR | 이미지 업로드 이후 확장 | 원본 업로드와 수동 기록, OCR 미구현 | 후속 범위 |
| 약국 찾기 | MVP는 Core API 없이 Kakao 지도 앱 딥링크만 사용 | 앱에 Kakao 지도 검색 링크가 있고, 병원·약국 좌표 조회는 Spring Core API의 Kakao Local 검색을 사용 | 단일 방식을 결정하기 전 현재 서버 검색을 제거하지 않음 |
| 공개 예약 코드 | `BD-` + 대문자 영문·숫자 6자리 | PostgreSQL UNIQUE·불변·정확 검색 계약은 PR #380에서 구현 중 | Firestore 매핑은 사용하지 않고 최신 `master`에서 migration·권한 계약 재검증 필요 |
| 동행 종료 | 가이드 12에서 `careEndedAt`, 가이드 13 일지 저장 뒤 `completedAt` | V18 코드가 `CARE_ENDED`·`COMPLETED`와 최초 서버 시각을 분리하고 중복 요청을 멱등 처리 | 방향 일치. DB 적용·Preview·실기기 검증은 남음 |
| 결제 증빙 | 가이드 8 증빙 업로드 | 선택 0~1개, JPEG·PNG·PDF, 파일당 10 MiB의 전용 테이블·Core API·Android 코드를 준비 | 실제 PG가 아닌 선택 증빙으로 구현. Storage 실업로드·보존 검증은 남음 |
| 처방 이미지 | 최대 3장 | 선택 0~3개, JPEG·PNG, 파일당 10 MiB의 전용 업로드·교체·삭제 코드를 준비 | OCR 없이 수동 이미지 보관으로 구현. 실기기 다중 선택 검증은 남음 |
| 매니저 일지 | 최대 300자 | 선택 입력, 최대 300자, 세션 완료와 리포트 실패·재시도 상태를 분리 | 방향 일치. 재시작·네트워크 실패 실검증은 남음 |
| 보호자 알림 | 매칭·상봉·종료만 보낼지 모든 가이드 진행을 보낼지 미정 | Core API FCM은 안심 채팅과 병원·약국 근접 위치 알림만 처리 | 이벤트별 수신자·채널·중복 억제 정책 필요 |
| 매니저 인증 파일 | 자격 증빙 1종, JPEG·PNG·WebP 1개·10 MiB | PR #383은 이미지 제한을 구현하지만 신분증·범죄경력을 필수로 둠 | 민감 원본 필수 조합 제거 전 병합 금지 |
| Core 업무 데이터 원본 | Firestore 중심 기술안 | 예약·세션·채팅·읽음·위치·리포트·후속 처리는 PostgreSQL 단일 쓰기 | 저장소 아키텍처 우선 |

## 2026-08-30 충돌 해소 결과

확정자·확정일이 없는 답변도 현재 구현, 법률상 최소수집과 되돌릴 수 있는지 여부를 함께 검토해 개발 기준을 정했다. 되돌릴 수 있는 제품값은 현재 검증된 구현을 유지하고, 민감수집·위치·권한·비가역 삭제는 미수집·비활성·fail-closed를 우선한다. 정책값을 정했다는 이유만으로 production 파기·위치·권한을 즉시 활성화하지 않는다.

| 항목 | 확인한 답변 | 현재 판단 |
| --- | --- | --- |
| P0-01 정보공유 동의 | 환자가 지정 보호자에게 정보 종류별 권한을 부여하고 철회하는 B안, 기획 확인 `2026-08-24` | 성인 환자의 세분화 동의 원칙은 확정. 의사결정능력이 제한된 환자의 대리권 증빙·승인은 추가 자문 필요 |
| P0-02 위치 | MVP 실시간 추적 제외, 4자리 상봉번호 제안 | GPS·상봉 1회 좌표 미수집으로 통일. production 강제 OFF 구현 필요 |
| P0-03 사고·업무범위 | 비의료 동행과 운영자 개입 C안, 보호자 실제 전화번호 연락 | 허용·금지 업무 원칙은 법률 의견과 일치. 사고 중단 상태, 연락 순서, 응답시간과 보상은 미결 |
| P0-04 결제 | UI 시뮬레이션, 무통장, 4만원·2시간 제안 | 실제 결제·개인계좌 안내를 운영 확정으로 보지 않음. 계약 주체, 환불·노쇼·정산과 약관 검토 필요 |
| P0-05 보관 | 일반 답변과 전용 정책의 기간 충돌 | 채팅 180일·채팅 첨부 30일·자격 증빙 원본 30일로 통일. production apply는 별도 게이트 |
| P0-06 가이드 | 가이드 2 영상 대체, 나머지 권장 단계표 승인 | 정상 종료·리포트 실패는 #307 구현에 반영했다. 사고·중단은 #297에 남김 |
| P1-07 매칭·공고번호 | 관리자 배정과 `BD-` 6자리 코드 | PostgreSQL 계약으로 통일. 선착순·Firestore 매핑 폐기. PR #380 재검증 필요 |
| P1-08 채팅·알림 | 직접 채팅과 첨부 조건 충돌 | 2,000자·JPEG/PNG/PDF 3개·각 10 MiB·읽음·FCM 계약과 `CARE_ENDED` 신규 입력 차단·매니저 조회 회수를 #382에 반영 |
| P1-09 매니저 서류 | PDF·100MB 다건 제안 | JPEG/PNG/WebP 1개·10 MiB 자격 증빙으로 축소, 신분증·범죄경력·건강 원본 미수집 |
| P1-10 내비게이션 | 공통 `홈 / 일정·이력 / 동행방 / 내 정보`, 예약별 참여관계 권한 | 상세 작업안은 있으나 확정자·확정일 없음 |
| P1-11 병원 안내 | 내부 길찾기 MVP 제외, 가이드 2 영상 대체 제안 | 촬영 허가, 비식별 검수, 교체 책임이 비어 있음 |
| P1-12 관리자 권한 | 최고관리자·운영관리자·개발자, MFA, 감사기록 1년 | 상세 작업안은 있으나 확정자·확정일과 실제 관리자 웹 구현 검증 필요. #349에서 추적 |
| P2-13 AI·OCR | MVP에서는 수동 입력, 실제 녹음·전송·처리 없음 | Notion 정책은 제외 방향이다. 법률상 외부 처리 통제 조건도 충족되지 않았으므로 현재 MVP에서 비활성 유지 |

세부 법률 추가질의는 [2026-08-25 정책·법률 정합성 점검](../reports/notion-policy-legal-alignment-2026-08-25.md), 이번 단일 기준 선택은 [2026-08-30 Notion 정책 충돌 해소 결과](../reports/notion-policy-conflict-resolution-2026-08-30.md)에 기록한다.

## MVP 기본값 결정 원칙

- 기존 기획 답변과 확정 정책은 그대로 유지한다.
- 법률·보안·민감정보·비가역 삭제·금전·외부 호환성과 충돌하지 않고 코드나 설정으로 되돌릴 수 있는 값은 환경과 무관하게 개발팀이 정할 수 있다.
- 선택 이유와 변경 위치를 Issue 또는 PR에 기록하되 임시라는 이유만으로 이슈를 열어 두지 않는다.
- 실제 개인정보의 수집·보관·삭제, 권한 확대, 비용 발생, 법정 의무 변경은 임시값 위임 대상이 아니다.
- 위험한 미결 항목은 비활성, dry-run 또는 fail-closed를 기본으로 한다.

개발팀 기본값 위임의 이전 근거는 [2026-08-28 MVP 정책값 경계 점검](../reports/notion-policy-team-response-alignment-2026-08-28.md), 현재 충돌 해소 결과는 [2026-08-30 Notion 정책 충돌 해소 결과](../reports/notion-policy-conflict-resolution-2026-08-30.md)에 기록한다.

## 확정해서 유지할 내용

- 예약·세션·채팅·읽음·리포트·후속 처리의 쓰기 원본은 PostgreSQL이다. 위치는 MVP production 미수집이다.
- 환자·보호자·매니저 앱은 Spring Core API를 거친다.
- 관리자 웹은 별도 Next.js 서버를 통해 같은 PostgreSQL을 본다.
- Firebase는 Auth, App Check, FCM, Storage와 결합 Functions에 사용한다.
- 인증 프로필·지원·매니저 서류 심사 메타데이터 일부는 전환 기간 Firestore에 남아 있고, 자격 증빙과 세션 첨부 파일 원본은 Firebase Storage에 둔다. 신규 심사의 상태·감사 메타데이터 쓰기 원본은 PostgreSQL로 이전한다.
- Realtime은 권위 있는 데이터 원본이 아니라 PostgreSQL 커밋 알림이다.
- OCR, 진료 녹음, STT, AI 요약은 시안이나 문서가 있어도 현재 완료 기능으로 표시하지 않는다.
- 화면별 상세 명세 안에서 음성·STT를 MVP 제외로 둔 문구와 `CLOVA Speech STT → Codex 재가공 실제 구현` 표기가 충돌한다. 저장소에 실제 녹음·STT·AI 처리 경로와 검증 증거가 생기기 전에는 미구현으로 유지한다.

## 사람 확인이 필요한 항목

아래 표는 개발팀이 임의로 확정하면 금전·법률·안전 책임이나 이용자 권리에 직접 영향을 주는 범위다.

| 우선순위 | 항목 | 결정해야 할 내용 | 개발 경계 |
| --- | --- | --- | --- |
| P0 | 환자→보호자 정보공유 동의 | 정보 종류별 동의, 철회, 대리권 확인 | 동의 모델 확정 전 열람 범위를 확대하지 않는다. |
| P0 | 진료 녹음·외부 AI 처리 | 환자 동의, 의료진 고지, 병원 규정, 위탁·국외이전, 원본 보관 기간 | 결정 전 녹음·STT·AI 처리를 활성화하지 않는다. |
| P0 | 취소·환불·노쇼·초과시간 | 매칭 전후 기준, 환불표, 현장 지연과 추가 결제 | 현재 상태 전이와 실제 금전 처리를 구분한다. |
| P0 | 사고 대응과 매니저 업무 범위 | 의료행위 금지, 사고보고, 보호자·119 연락, 보험 | 앱 문구가 진단·판단·복약지도로 보이지 않게 한다. |
| P1 | 가이드 단계 정규화 | Figma 1~13과 DB 가이드 단계의 명칭·완료 이벤트 | 화면 번호를 서버 상수로 고정하지 않는다. |
| P1 | Figma 문구 정리 | 역할이 뒤섞인 홈 문구, 권한 필수 표기, 샘플 이름·날짜 | 정리 전 문구를 제품 정책으로 복제하지 않는다. |
| P1 | 약국 검색 방식 (#314) | Kakao 앱 딥링크만 유지할지 Core API 검색과 지도 링크를 함께 유지할지 | 결정 전 이미 구현된 서버 검색 경로를 제거하지 않는다. |
| P1 | 보호자 알림 범위 (#299) | 매칭·상봉·종료 핵심 이벤트 또는 모든 단계, 수신자·채널·중복 기준 | 현재 채팅·위치 알림 외 이벤트를 임의로 발송하지 않는다. |
| P1 | 결제·처방 파일 (#307) | 결제 증빙 최소 장수와 파일 한도, 처방 이미지 최소 1장 여부·최대 3장·파일 한도 | 채팅 첨부 정책을 재사용하려면 용도별 보관·권한 계약을 먼저 분리한다. |

## 확정 뒤 남은 구현 차이

| 항목 | 확정 기준 | 남은 구현 |
| --- | --- | --- |
| 위치 | MVP production GPS 미수집 | 앱 UI 숨김과 Core API `off` 모드 강제. `CARE_ENDED` 이후 write 거부는 #382에서 완료 |
| 건강정보·리포트 보관 | 개발 구현 후보: 구조화 건강정보는 `CARE_ENDED`·취소 후 1년, 세션이 없으면 예약일 후 1년. 최종 리포트는 `COMPLETED`·취소 후 1년 | production 법률 대조 뒤 만료 필드·조회 차단·삭제 job 적용 |
| 공고번호 | `BD-` + 6자리, 관리자 배정, PostgreSQL 원본 | PR #380 충돌 해소와 선행 migration 뒤 재검증 |
| 매니저 서류 | 자격 증빙 1종, JPEG·PNG·WebP 1개·10 MiB | PR #383의 신분증·범죄경력 필수 조합 제거, legacy 키 이관 |
| 파기 | 채팅 180일·채팅 첨부 30일·자격 원본 30일 | production 처리방침·책임자·복원 검증 뒤 apply 활성화 |

## 문서 정리 판단

- Notion은 제품 의도와 미결 정책의 기준으로 유지한다.
- Notion의 Firestore 컬렉션명, Functions 처리와 구현 완료 표기는 현재 코드의 기술 사실을 대체하지 않는다.
- Notion 문서의 마지막 편집 시각이 저장소 정합성 점검 이후라면 변경된 항목을 다시 대조한다. 되돌릴 수 있는 기본값과 별도 production 게이트는 [MVP 정책 기본값 결정 경계](mvp-policy-default-boundary.md)에 따라 구분한다.
- 오래된 로컬 기능설명서 PDF는 초기 범위 스냅샷으로 낮춘다.
- 구현 상태는 [현재 구현 상태](../status/implementation-status.md), 기술 구조는 [목표 인프라 구조](../architecture/target-infrastructure.md)를 따른다.
