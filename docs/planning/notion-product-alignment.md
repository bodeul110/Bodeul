# Notion 제품 기준 정합성

기준일: 2026-09-21

이번 확인은 Notion 본문·블록 댓글, Figma MVP 메타데이터, 양쪽 저장소 코드와 GitHub 병합 상태를 대조한 것이다. 서버·DB·실기기의 종단 검증을 새로 수행한 기록은 아니다.

## 확인 범위

Notion의 MVP 통합본 v1.1, 화면별 상세 명세 v2.2, 동행 채팅 명세 v1.0, 출시 전 법률 체크리스트 v1.0, `개발 정책 관련 내용`, 보유기간 및 파기정책 v1.0을 현재 저장소 구현과 대조했다. 2026-08-25에는 기획 답변과 2026-08-11 법률 검토 의견서를, 2026-09-01에는 위치·결제·가이드 2 후속 기획 답변을 다시 확인했다. 2026-09-03에는 기존 매니저 위치 경로의 production 차단 경계를 코드 변경 기준으로 갱신했다. 2026-09-13에는 `개발 정책 관련 내용`의 2026-09-09 답변에 맞춰 보호자 예약, STT 내부 개발과 관리자 보안 기준을 갱신했다. 같은 날 `보들 앱 화면 피드백` 원문과 예시 이미지도 내부 테스트 결과로 대조하고 후속 이슈에 연결했다. 이번 갱신에서 배포·실기기·DB 상태를 직접 재검증한 것은 아니며 기존 구현 증적과 팀의 테스트 의견을 구분해 유지한다. 비공개 URL, 페이지 ID, 계정 정보, 개인정보와 법률 문서 원본은 이 공개 저장소에 기록하지 않는다.

## 충돌 판정 기준

- 제품 목표가 충돌하면 날짜와 답변자가 확인되는 최신 기획 답변을 기존 기획 문서와 Figma보다 우선한다.
- 법률·보안·외부 서비스 약관과 이미 배포된 데이터 계약은 기획 답변만으로 완화하지 않는다. 실제 수집·결제·삭제·권한 확대는 별도 운영 게이트를 통과해야 한다.
- 현재 구현 여부는 저장소의 코드·API·DB migration과 검증 기록으로 판단한다. 기획 답변이나 Figma 화면이 있다는 이유로 구현 완료로 표시하지 않는다.
- 개발자 제안은 위 기준에 답이 없고 되돌릴 수 있는 MVP 기본값에만 사용한다.

## 현재 제품 흐름

1. 첫 화면과 권한 안내를 거쳐 환자·보호자 또는 매니저 역할을 선택한다.
2. 매니저는 자격 서류 제출과 승인 단계를 거친다.
3. 성인 환자가 병원, 일정, 건강·이동 정보를 입력해 동행을 요청한다. 보호자의 환자 대상 예약 생성과 환자 동의 범위 내 진행·최종 리포트 조회도 제품 목표로 포함한다. 예약 생성 사실만으로 정보 전체 접근권한을 주지 않으며, 새 구현 차이는 [#419](https://github.com/bodeul110/Bodeul/issues/419)에서 추적한다.
4. 관리자가 승인된 매니저 한 명을 배정한다. 승인 매니저의 공개 일감 목록과 선착순 수락은 MVP에서 제외한다.
5. 매칭 이후 매니저는 동행 가이드를 진행하고 환자·보호자는 진행 상태를 확인한다.
6. 환자·배정 매니저와 `CHAT` 정보공유 동의가 유효한 보호자는 직접 채팅하고 주요 상태 알림을 받는다. 보호자의 첨부 이용에는 `CHAT + ATTACHMENT`가 모두 필요하다. 시스템 이벤트 중심 동행방은 과거 제안으로 분류한다.
7. 서비스 종료 후 최종 리포트, 후기와 정산 확인을 제공한다. 별도 SOS·자동 연락·사고 기록은 MVP에서 제공하지 않는다.

Figma에는 동행 가이드 1~13 화면이 있으나, 현재 서버 계약은 병원 가이드 데이터의 단계 수를 사용한다. `13단계 고정`은 아직 기술 계약이 아니다.

## 현재 구현과의 정합성

| 항목 | Notion 제품 의도 | 현재 저장소 기준 | 판단 |
| --- | --- | --- | --- |
| 예약·매칭 | 관리자 배정 | PostgreSQL 배정 함수는 관리자 runtime만 실행하며 매니저 self-accept API는 없음 | 관리자 배정으로 통일, 선착순 수락 폐기 |
| 동행 가이드 | 단계별 기록과 보호자 진행 확인, 가이드 2는 영상 안내 | DB 가이드 snapshot·Core API에 V21 영상 메타데이터 검증과 텍스트 fallback 계약이 있음 | 가이드 2 영상은 목표로 채택하되 승인된 자산·사용 권리·비식별 검수 전에는 운영 노출 금지 |
| 동행 채팅 | 환자·배정 매니저·`CHAT` 동의 보호자 직접 채팅 | 직접 채팅, 읽음, FCM, Realtime 구현 | `CARE_ENDED`부터 신규 입력과 매니저 조회 차단, 환자와 유효한 `CHAT` 동의 보호자만 기존 본문 조회 |
| 채팅 첨부 | JPEG·PNG·PDF 최대 3개·각 10 MiB | Core API가 MIME·magic byte·용량·경로와 보호자의 `CHAT + ATTACHMENT` 동의를 검증 | 동행 종료·취소 후 30일 보관과 `CARE_ENDED` 신규 첨부 차단 구현 완료. 보호자는 두 동의가 모두 유효한 동안만 조회 |
| 위치 공유 | 환자 GPS 좌표를 1분 주기로 공유하고 4자리 상봉번호는 사용하지 않음 | 기존 매니저 단말 1회·연속 경로는 Android·Core API 기본 `OFF`, release·production 하드코딩 `false`, debug·Preview 명시적 opt-in이다. Core `OFF`는 좌표 read/write·legacy PATCH를 거부하고 저장값을 마스킹하며 Core FCM·legacy Functions 알림도 중단한다 | 기존 경로의 production 노출 차단만 완료. 환자 단말 1분 수집·동의·철회·보호자별 인가·즉시 삭제와 24시간 최종 삭제는 별도 미구현 |
| 보호자 예약 | 보호자 예약 생성과 환자 동의 범위의 진행·최종 리포트 조회 | 기존 환자 본인 전용 생성·보호자 제한 조회 계약과의 차이 확인 필요 | [#419](https://github.com/bodeul110/Bodeul/issues/419)에서 구현. 수정·취소 및 건강정보·위치·채팅·첨부 권한의 일괄 확대는 아님 |
| 환자·보호자 연결 | 성인 환자가 지정 보호자에게 범위별 권한을 부여하고 즉시 철회 | V17·Core API·Android에 동의·철회·기본 거부 계약 구현 | 성인 환자 흐름은 #381 병합 완료. 의사결정능력이 제한된 환자의 대리권은 추가 자문 필요 |
| 취소 | 환자 취소와 무통장입금 수동 처리 | 환자 본인의 `REQUESTED`, `MATCHED` 취소와 관리자 결제 전이 계약 존재 | 실제 환불·노쇼·정산 운영은 별도 게이트 |
| 보관 기간 | 위치 원본 최대 24시간, 채팅 180일, 채팅 첨부 30일, 자격 증빙 원본 30일 | 같은 데이터별 파기 계약과 dry-run/apply 경로 구현 | 기간은 유지하되 production 위치 수집과 삭제 apply는 각각 별도 출시 게이트 대기 |
| 음성·STT | 네이버 클라우드 STT 내부 연동, 녹음 시작·정지와 정지 후 자동 저장 | 새 개발 범위이며 이번 정책 갱신으로 구현 완료를 주장하지 않음 | [#420](https://github.com/bodeul110/Bodeul/issues/420)에서 비식별 테스트 데이터로 개발. 실제 환자 데이터 처리와 운영 노출은 별도 게이트 |
| OCR·AI 요약 | OCR 제외, 약국 영수증 사진 저장만 유지. AI 요약·복약 비교·최종 리포트 자동 반영은 이번 채택 범위가 아님 | 원본 업로드와 수동 기록을 자동 인식·AI 처리로 표시하지 않음 | #30·#31의 과거 보류 기록 유지. STT 내부 개발과 구분 |
| 약국 찾기 | 가이드 9 CTA는 Kakao 지도 앱 딥링크 사용 | 앱 딥링크와 별개로 병원·약국 좌표 조회용 Spring Core API Kakao Local 검색을 유지 | #314와 PR #329 완료. 두 경로의 책임을 합치거나 서버 검색을 제거하지 않음 |
| 공개 예약 코드 | `BD-` + 대문자 영문·숫자 6자리 | PostgreSQL V19·Core API·Android PR #380과 관리자 정확 검색 admin-web PR #43이 모두 병합됨 | Firestore 매핑은 사용하지 않고 신청자·배정 매니저 표시 경계와 관리자 감사 검색을 유지. #315 완료 |
| 동행 종료 | 가이드 12에서 `careEndedAt`, 가이드 13 일지 저장 뒤 `completedAt` | V18 코드가 `CARE_ENDED`·`COMPLETED`와 최초 서버 시각을 분리하고 중복 요청을 멱등 처리 | 방향 일치. 코드 병합·migration-contract 검증 기록과 환경별 DB 적용·실기기 검증을 구분. 현재 가용성은 #429에서 재확인 |
| 결제 증빙 | 가이드 8 증빙 업로드 | 선택 0~1개, JPEG·PNG·PDF, 파일당 10 MiB의 전용 테이블·Core API·Android 코드를 준비 | 실제 PG가 아닌 선택 증빙으로 구현. Storage 실업로드·보존 검증은 남음 |
| 처방 이미지 | 최대 3장 | 선택 0~3개, JPEG·PNG, 파일당 10 MiB의 전용 업로드·교체·삭제 코드를 준비 | OCR 없이 수동 이미지 보관으로 구현. 실기기 다중 선택 검증은 남음 |
| 매니저 일지 | 최대 300자 | 선택 입력, 최대 300자, 세션 완료와 리포트 실패·재시도 상태를 분리 | 방향 일치. 재시작·네트워크 실패 실검증은 남음 |
| 보호자 알림 | 참여자 직접 채팅·읽음·일반 FCM을 사용하고 단계별 시스템 메시지·알림톡은 제외 | Core API FCM은 안심 채팅과 병원·약국 근접 위치 알림만 처리 | #299 결정과 현재 구현 범위를 유지하고 임의의 단계 알림을 추가하지 않음 |
| 매니저 인증 파일 | 자격 증빙 1종, JPEG·PNG·WebP 1개·10 MiB | 메인 PR #383이 병합돼 `license \| nursingLicense` 1종 계약과 legacy 이관을 구현 | 관리자 웹 PR #44도 병합됨. legacy 파일 정리와 실제 환경의 권한·열람 검증은 #313에서 별도 확인 |
| Core 업무 데이터 원본 | Firestore 중심 기술안 | 예약·세션·채팅·읽음·위치·리포트·후속 처리는 PostgreSQL 단일 쓰기 | 저장소 아키텍처 우선 |
| 사고 대응 | 비의료 동행 C안, 별도 SOS·자동 연락·사고보고·사고 상태·보험은 MVP 제외 | Android 실행 경로와 저장소 writer를 제거하고 Core API 신규 escalation 쓰기를 거부. 기존 열·필드·파서는 legacy 읽기 전용 유지 | #297 기준으로 비활성화, 일반 취소·정상 동행 종료 유지 |

## 2026-08-30 충돌 해소와 2026-09-01 후속 결정

확정자·확정일이 없는 답변도 현재 구현, 법률상 최소수집과 되돌릴 수 있는지 여부를 함께 검토해 개발 기준을 정했다. 2026-09-01 후속 기획 답변은 위치·무통장입금·가이드 2의 제품 목표를 대체하지만, 정책값을 정했다는 이유만으로 production 위치 수집·금전 수취·파기·권한을 즉시 활성화하지 않는다.

| 항목 | 확인한 답변 | 현재 판단 |
| --- | --- | --- |
| P0-01 정보공유 동의 | 환자가 지정 보호자에게 정보 종류별 권한을 부여하고 철회하는 B안, 기획 확인 `2026-08-24` | 성인 환자의 세분화 동의 원칙은 확정. 의사결정능력이 제한된 환자의 대리권 증빙·승인은 추가 자문 필요 |
| P0-02 위치 | 환자 GPS 좌표를 1분 주기로 공유하고 4자리 상봉번호는 제외 | 제품 목표로 채택. 기존 매니저 GPS 경로는 앱·서버 기본 `OFF`와 release·production 하드코딩 `false`로 닫았지만, 환자 수집·열람 계약은 별도로 구현하고 동의·철회·인가·파기 종단 검증 전 production `OFF`를 유지 |
| P0-03 사고·업무범위 | 비의료 동행과 운영자 개입 C안. MVP에서는 별도 SOS, 119·보호자·관리자 자동 연락, 사고보고·사고 전용 상태, 보험·자동 보상을 제외 | #297에서 신규 SOS 실행·쓰기를 비활성화하고 legacy 필드는 읽기 호환으로 유지. 기존 일반 예약 취소와 정상 동행 종료만 유지하며 진행 중 사고 전용 상태는 만들지 않음 |
| P0-04 결제 | 실제 무통장입금을 목표 방식으로 사용 | 제품 목표로 채택. 운영 주체·입금 계좌·입금 확인·환불·노쇼·정산 기준과 사용자 안내가 준비되기 전에는 계좌 노출·실제 수취·입금 완료 처리를 `OFF`로 유지 |
| P0-05 보관 | 일반 답변과 전용 정책의 기간 충돌 | 채팅 180일·채팅 첨부 30일·자격 증빙 원본 30일로 통일. production apply는 별도 게이트 |
| P0-06 가이드 | 가이드 2는 영상으로 대체하고 나머지 권장 단계표 사용 | 영상 목표를 채택하되 자산 원본·촬영 및 사용 권리·환자와 의료진 비식별 검수·교체 책임이 확인되기 전에는 텍스트 안내를 유지. 정상 종료·리포트 실패 구현 #307은 종료됨 |
| P1-07 매칭·공고번호 | 관리자 배정과 `BD-` 6자리 코드 | PostgreSQL 계약으로 통일하고 선착순·Firestore 매핑을 폐기했다. 메인 PR #380과 관리자 웹 PR #43이 병합됐고 #315를 완료 처리함 |
| P1-08 채팅·알림 | 직접 채팅과 첨부 조건 충돌 | 2,000자·JPEG/PNG/PDF 3개·각 10 MiB·읽음·FCM 계약과 `CARE_ENDED` 신규 입력 차단·매니저 조회 회수를 반영한 PR #382가 병합됨 |
| P1-09 매니저 서류 | PDF·100MB 다건 제안 | JPEG/PNG/WebP 1개·10 MiB 자격 증빙으로 축소, 신분증·범죄경력·건강 원본 미수집 |
| P1-10 내비게이션 | 공통 `홈 / 일정·이력 / 동행방 / 내 정보`, 예약별 참여관계 권한 | #305·PR #376의 완료 기록은 유지. 9월 9일 추가된 보호자 예약 생성은 #419로 분리하고 다중 환자 UI·복잡한 딥링크 예외는 제외 |
| P1-11 병원 안내 | 내부 길찾기 MVP 제외, 가이드 2 영상 대체 | 영상 목표는 확정. 승인된 자산·촬영 및 사용 권리·비식별 검수·교체 책임 확보를 운영 게이트로 두고 [#391](https://github.com/bodeul110/Bodeul/issues/391)에서 구현을 추적 |
| P1-12 관리자 권한 | 최고관리자·운영관리자·개발자, MFA, 감사기록 1년 | 9월 9일 답변으로 승인·회수·유휴 세션·긴급 접근·감사 필드를 구체화해 #134에 반영. 기존 역할 구현 기록과 실제 운영 검증을 구분하며 App Check는 별도 검증 |
| P2-13 AI·OCR | 당시에는 수동 입력과 실제 녹음·전송·처리 제외 | 9월 9일 답변으로 STT 내부 개발만 #420에 새로 채택. 사용자용 수동 입력, OCR·AI 요약 제외와 실제 환자 데이터 운영 게이트는 유지 |

세부 법률 추가질의는 [2026-08-25 정책·법률 정합성 점검](../reports/notion-policy-legal-alignment-2026-08-25.md), 이번 단일 기준 선택은 [2026-08-30 Notion 정책 충돌 해소 결과](../reports/notion-policy-conflict-resolution-2026-08-30.md)에 기록한다.

## 2026-09-09 기획 답변의 적용 범위

아래는 2026-09-13에 채택한 최신 제품·운영 기준이다. 앞선 문서의 보호자 예약 전면 제외와 STT 개발 전면 보류 문구보다 우선한다. 정책을 채택했다는 사실은 구현 또는 실제 운영 설정 완료를 뜻하지 않는다.

| 항목 | 채택한 기준 | 작업 경계 |
| --- | --- | --- |
| 보호자 예약·내비게이션 | 보호자 예약 생성, 환자 동의 범위의 진행·최종 리포트 조회, 공통 하단 탭과 예약별 참여관계 권한 | #419. 생성 권한과 정보공유 동의를 분리하고 `CHAT`, `CHAT + ATTACHMENT` 경계를 유지. 권한 없는 화면은 안내 후 이전·홈으로 복귀. 다중 환자 관리·상세 환자 전환 UI·복잡한 딥링크 예외 제외 |
| 음성·STT | 네이버 클라우드 STT 내부 연동, 녹음 시작·정지와 정지 시 자동 저장 | #420. 기존 Spring `core-api/`에서 공급자 비밀값과 세션 인가를 처리하고 원본 파일·PostgreSQL 메타데이터를 구분. 실제 환자 데이터 없는 내부 테스트와 운영 활성화 분리 |
| 관리자 역할 | `SUPER_ADMIN`이 `SUPER_ADMIN / OPERATIONS / DEVELOPER` 부여·변경·회수 승인 | #134. Firebase custom claims와 관리자 서버 권한 매핑 검증. 임시 권한은 작업 완료 즉시 회수하고 역할·팀 변경 시 접근권한 즉시 점검 |
| 관리자 세션·긴급 접근 | 모든 관리자 MFA, 유휴 자동 로그아웃, 긴급 접근 사유·임시 부여·사후 검토 | #134. 정확한 로그아웃 시간과 정기 접근권한 점검 주기는 실제 운영 전 확정. App Check 검증과 혼동하지 않음 |
| 관리자 감사 | 작업자 ID·역할·시각·대상 정보·행위(조회·다운로드·수정·삭제)·사유·결과, 1년 보관 | #134. 원본 민감정보를 로그에 복사하지 않으며 코드 존재와 실제 저장·조회·보관 검증을 구분 |

- 작업 목적: 새 기획 답변을 이전 보류 문구 때문에 놓치지 않도록 제품 범위와 실행 작업을 맞춘다.
- 선택한 방식: 새 기능 차이는 #419·#420, 관리자 운영 검증은 기존 #134에서 관리하고 완료된 이슈는 다시 열지 않는다.
- 대안: 기존 이슈를 모두 다시 열거나 정책 답변만으로 운영을 활성화하는 방식은 과거 완료 근거와 검증 수준을 혼동시킨다.
- 선택 이유: 현재 MVP 규모에서는 기존 앱·Spring Core API·관리자 서버 경계를 재사용하고 필요한 변경만 추적하는 편이 운영 부담이 적다.
- 리스크: 예약 신청자와 환자 혼동, 민감한 음성 전송, 오래된 관리자 세션의 잔여 권한은 구현·운영 검증에서 각각 확인한다.

## 2026-09-13 내부 앱 테스트 피드백 정리

Notion `보들 앱 화면 피드백`은 디자인 제안서가 아니라 팀의 내부 앱 테스트 결과물로 분류한다. 원문과 메모 출력 예시 이미지를 확인했으며, 2026-09-13은 이번 대조일이지 테스트 실시일이 아니다. 문서에는 테스트 빌드, 기기·OS, Mock/Core API 모드, 세션의 기존 진행 이력이 없어 현재 버전에서의 재현 여부는 후속 작업에서 확인한다.

| 내부 테스트에서 나온 내용 | 판단과 후속 작업 | 우선순위 |
| --- | --- | --- |
| 로딩·동행 신청 검색에 긍정 의견, 환자 화면은 예약까지 별도 지적 없음 | 해당 테스트 범위의 사용성 의견으로 기록. 전체 환자·보호자 흐름이나 production E2E 통과로 확대하지 않음 | 기록 |
| 매니저 가이드가 step2부터 시작함 | [#422](https://github.com/bodeul110/Bodeul/issues/422). 새 세션과 상봉 완료 후 재진입, 기존 7단계와 현재 가이드 snapshot을 구분해 확인. 아직 재현 확정 아님 | 높음 |
| step2~7 메모를 마지막에 메모장처럼 나열 | [#423](https://github.com/bodeul110/Bodeul/issues/423), PR #431 병합. 단계 메모 모아보기 구현, AI 요약·300자 최종 일지와 분리. 열린 이슈의 후속 검증과 코드 반영을 구분 | 높음 |
| 카카오맵으로 매니저 자신의 현재 위치 확인 | [#424](https://github.com/bodeul110/Bodeul/issues/424), PR #430 병합. 현재 위치 지도 확인 구현. 서버 좌표 저장·보호자 공유나 production 위치 flag 활성화는 아님 | 중간 |
| step4에 STT·음성 저장, 추후 step3~4 사이 배치 | 새 이슈를 만들지 않고 [#420](https://github.com/bodeul110/Bodeul/issues/420)에 추가. 피드백의 번호와 실제 stepCode를 대조해 진입 위치 결정 | 중간 |
| 화자 분리·보호자 질문 추출·AI 2차 가공 실험 | 팀의 별도 기술 실험 맥락. 앱 구현·연동 검증·운영 승인으로 표시하지 않으며 자동 AI 리포트로 범위를 확장하지 않음 | 별도 연구 |

피드백의 `step2~7`과 Figma의 가이드 1~13을 같은 순번 체계라고 가정하지 않는다. 기존 #301·#306·#307의 완료 기록과 현재 세션 snapshot을 보존하고, 새 진입 문제와 화면 개선만 별도 추적한다. #419·#420·#422·#423·#424는 `BoDeul 작업 백로그`에 연결했으며 Android 영역과 우선순위를 설정했다. 9월 21일 재확인 시 #430·#431의 코드 병합은 완료됐지만 연결 이슈는 열려 있다. `내부테스트 2`는 제목만 있어 새 재현·완료 증거로 쓰지 않는다.

- 작업 목적: 내부 테스트 결과를 실제 수정·확인 가능한 단위로 추적한다.
- 선택한 방식: 재현 확인, 메모 모아보기, 자기 위치 확인을 분리하고 STT는 기존 #420에 합친다.
- 대안: 모든 의견을 새 통합 이슈나 AI 리포트 작업으로 묶으면 STT와 무관한 개선까지 대기하게 된다.
- 선택 이유: 현재 MVP 규모에서는 첫 진입의 혼란과 수동 기록 확인을 먼저 개선하고, 외부 공급자 연동은 독립적으로 진행하는 편이 검증 범위가 작다.
- 리스크: 오래된 테스트 세션의 순번 오해, 합친 메모의 잘림, CARE_ENDED 이후 회수된 정보의 재노출을 각각 확인한다.

## MVP 기본값 결정 원칙

- 기존 기획 답변과 확정 정책은 그대로 유지한다.
- 법률·보안·민감정보·비가역 삭제·금전·외부 호환성과 충돌하지 않고 코드나 설정으로 되돌릴 수 있는 값은 환경과 무관하게 개발팀이 정할 수 있다.
- 선택 이유와 변경 위치를 Issue 또는 PR에 기록하되 임시라는 이유만으로 이슈를 열어 두지 않는다.
- 실제 개인정보의 수집·보관·삭제, 권한 확대, 비용 발생, 법정 의무 변경은 임시값 위임 대상이 아니다.
- 위험한 미결 항목은 비활성, dry-run 또는 fail-closed를 기본으로 한다.

개발팀 기본값 위임의 이전 근거는 [2026-08-28 MVP 정책값 경계 점검](../reports/notion-policy-team-response-alignment-2026-08-28.md), 현재 충돌 해소 결과는 [2026-08-30 Notion 정책 충돌 해소 결과](../reports/notion-policy-conflict-resolution-2026-08-30.md)에 기록한다.

## 확정해서 유지할 내용

- 예약·세션·채팅·읽음·리포트·후속 처리의 쓰기 원본은 PostgreSQL이다. 기존 매니저 위치 경로는 기본 `OFF`이고 release·production에서는 활성화하지 않는다. 환자 GPS 1분 공유는 제품 목표지만 별도 경로를 구현하고 production 게이트를 통과하기 전에는 수집·공유하지 않는다.
- 환자·보호자·매니저 앱은 Spring Core API를 거친다.
- 관리자 웹은 별도 Next.js 서버를 통해 같은 PostgreSQL을 본다.
- Firebase는 Auth, App Check, FCM, Storage와 결합 Functions에 사용한다.
- 인증 프로필·지원·매니저 서류 심사 메타데이터 일부는 전환 기간 Firestore에 남아 있고, 자격 증빙과 세션 첨부 파일 원본은 Firebase Storage에 둔다. 현재 자격 서류 심사 상태는 Firestore 계약을 유지한다. PostgreSQL의 관리자 RBAC·감사와 outbox를 함께 사용하며, 이를 심사 도메인 전체 이전 완료로 표시하지 않는다.
- Realtime은 권위 있는 데이터 원본이 아니라 PostgreSQL 커밋 알림이다.
- OCR, 진료 녹음, STT, AI 요약은 시안이나 문서가 있어도 현재 완료 기능으로 표시하지 않는다.
- 음성·STT 전면 제외와 `CLOVA Speech STT → Codex 재가공 실제 구현` 표기는 9월 9일의 STT 내부 개발 범위로 대체한다. 기존 #30의 전체 AI 리포트 보류와 새 #420을 구분하며, 실제 연동 증거 전에는 완료로 표시하지 않는다.

## 운영 활성화 전 확인이 필요한 항목

아래 표는 개발팀이 임의로 확정하면 금전·법률·안전 책임이나 이용자 권리에 직접 영향을 주는 범위다.

| 우선순위 | 항목 | 결정해야 할 내용 | 개발 경계 |
| --- | --- | --- | --- |
| P0 | 의사결정능력이 제한된 환자의 대리권 | 법정대리 관계 증빙, 승인 주체와 철회·감사 절차 | 성인 환자 본인 동의 계약을 대리권으로 확대하지 않는다. |
| P0 | 위치 production 활성화 | 환자 위치 수집 동의·즉시 철회, 공유 대상, 역할별 인가, 24시간 이내 원본 파기와 삭제 실패 처리 | 기존 매니저 경로는 앱·서버 기본 `OFF`와 release·production `false`로 차단한다. 향후 환자 경로도 모든 종단 검증을 통과하기 전 권한 요청·좌표 전송·API 저장·열람을 `OFF`로 유지한다. |
| P0 | 무통장입금 운영 | 운영 주체 명의 계좌, 입금 확인 주체·상태, 환불·노쇼·정산과 사용자 안내 | 준비 전 계좌를 노출하거나 실제 금액을 받지 않고 현재 결제 시뮬레이션을 운영 결제로 표시하지 않는다. |
| P0 | 실제 환자 음성·외부 처리 운영 활성화 | 환자 동의, 의료진 고지, 병원 규정, 위탁·국외이전, 원본 보관 기간과 공급자 사용 조건 | 게이트 전 실제 환자 녹음 수집·전송은 비활성. #420의 비식별 테스트 데이터 기반 내부 개발 전체를 막지는 않는다. AI 요약은 이번 범위가 아니다. |
| P0 | 취소·환불·노쇼·초과시간 | 매칭 전후 기준, 환불표, 현장 지연과 추가 결제 | 현재 상태 전이와 실제 금전 처리를 구분한다. |
| P0 | 매니저 업무 범위와 운영 안내 | 비의료 동행 범위와 사용자 안내 | 별도 SOS·자동119·사고 전용 상태·보험은 MVP 제외 결정 유지. 앱 문구가 진단·복약지도로 보이지 않게 한다. |
| P1 | 가이드 단계 정규화 | Figma 1~13과 DB 가이드 단계의 명칭·완료 이벤트 | 화면 번호를 서버 상수로 고정하지 않는다. |
| P1 | 가이드 2 영상 자산 ([#391](https://github.com/bodeul110/Bodeul/issues/391)) | 승인된 영상 원본, 촬영·사용 권리, 환자·의료진 비식별 검수와 교체 책임 | 게이트 통과 전 외부 URL이나 미승인 파일을 앱에 연결하지 않는다. |
| P1 | Figma 문구 정리 | 역할이 뒤섞인 홈 문구, 권한 필수 표기, 샘플 이름·날짜 | 정리 전 문구를 제품 정책으로 복제하지 않는다. |

## 확정 뒤 남은 구현 차이

| 항목 | 확정 기준 | 남은 구현 |
| --- | --- | --- |
| 보호자 예약 | 보호자 예약 생성과 환자 동의 범위의 진행·최종 리포트 조회 | #419에서 신청자·환자 구분, 범위별 인가와 동의 철회·만료, 앱 복귀 흐름을 구현·검증 |
| STT 내부 연동 | 네이버 클라우드 연동, 녹음 시작·정지·자동 저장 | #420에서 비식별 음성 기반 저장·전사·실패 재시도를 구현. mock 검증과 실제 공급자 연동을 구분 |
| 관리자 보안 | 최고관리자 승인, MFA·유휴 로그아웃·즉시 권한 회수, 감사기록 1년 | #134의 새 검증 목록으로 실제 계정·세션·감사 경계를 확인. 이번 문서 변경으로 검증 완료 처리하지 않음 |
| 위치 | 환자 GPS 좌표 1분 공유, 4자리 상봉번호 제외 | 기존 매니저 단말·10초 또는 15m 경로의 Android·Core API 기본 `OFF`와 release·production `false`는 반영. 남은 범위는 이를 재사용하지 않고 환자 주체·1분 서버 제한·동의·철회·보호자별 인가·종료 즉시 삭제와 24시간 최종 상한을 별도로 구현·종단 검증하는 것 |
| 무통장입금 | 실제 무통장입금을 목표 방식으로 사용 | V22·V23, Core API·Android·관리자 UI와 Preview fixture 검증은 반영됨. 실제 계좌·수취·환불 운영 활성화만 별도 게이트 |
| 가이드 2 영상 | 승인된 영상으로 안내 | [#391](https://github.com/bodeul110/Bodeul/issues/391)에서 V21 메타데이터·fallback 계약 위에 승인 자산과 플레이어를 연결. 자산·권리·비식별 게이트 통과 전 노출 금지 |
| 건강정보·리포트 보관 | 개발 구현 후보: 구조화 건강정보는 `CARE_ENDED`·취소 후 1년, 세션이 없으면 예약일 후 1년. 최종 리포트는 `COMPLETED`·취소 후 1년 | production 법률 대조 뒤 만료 필드·조회 차단·삭제 job 적용 |
| 공고번호 | `BD-` + 6자리, 관리자 배정, PostgreSQL 원본 | 메인 PR #380과 관리자 웹 PR #43 병합, #315 완료 |
| 매니저 서류 | 자격 증빙 1종, JPEG·PNG·WebP 1개·10 MiB | 메인 #383·관리자 웹 #44 병합 완료. legacy 정리와 환경별 권한 검증은 #313, 운영 개방은 #134 |
| 파기 | 채팅 180일·채팅 첨부 30일·자격 원본 30일 | production 처리방침·책임자·복원 검증 뒤 apply 활성화 |

## 문서 정리 판단

- Notion은 제품 의도와 미결 정책의 기준으로 유지한다.
- Notion의 Firestore 컬렉션명, Functions 처리와 구현 완료 표기는 현재 코드의 기술 사실을 대체하지 않는다.
- Notion 문서의 마지막 편집 시각이 저장소 정합성 점검 이후라면 변경된 항목을 다시 대조한다. 되돌릴 수 있는 기본값과 별도 production 게이트는 [MVP 정책 기본값 결정 경계](mvp-policy-default-boundary.md)에 따라 구분한다.
- 오래된 로컬 기능설명서 PDF는 초기 범위 스냅샷으로 낮춘다.
- 구현 상태는 [현재 구현 상태](../status/implementation-status.md), 기술 구조는 [목표 인프라 구조](../architecture/target-infrastructure.md)를 따른다.
