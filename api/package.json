{
  "name": "bodeul-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/*.test.js",
    "check": "npm run typecheck && npm run build && node --test dist/*.test.js",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "@types/pg": "8.20.0",
    "typescript": "~6.0.2"
  },
  "dependencies": {
    "firebase-admin": "14.1.0",
    "pg": "8.22.0"
  }
}
